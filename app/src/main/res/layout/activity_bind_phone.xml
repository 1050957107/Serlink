<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true">

    <LinearLayout
        android:id="@+id/ll_bind_input_phone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="69dp"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@+id/ll_back"
        tools:ignore="MissingConstraints">

        <include layout="@layout/layout_login_phone_input" />

    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="30dp"
        android:orientation="vertical"
        app:layout_constraintTop_toBottomOf="@+id/tv_bind_submit"
        tools:ignore="MissingConstraints">

        <include layout="@layout/layout_login_agreement" />
    </LinearLayout>

    <TextView
        android:id="@+id/tv_bind_submit"
        android:layout_width="match_parent"
        android:layout_height="45dp"
        android:layout_marginLeft="60dp"
        android:layout_marginTop="44dp"
        android:layout_marginRight="60dp"
        android:background="@drawable/color_mian_radius8"
        android:gravity="center"
        android:text="@string/input_get_code"
        android:textColor="@color/white"
        android:textSize="16sp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toBottomOf="@+id/ll_bind_input_phone"
        tools:ignore="MissingConstraints" />


    <include layout="@layout/layout_back" />

</androidx.constraintlayout.widget.ConstraintLayout>